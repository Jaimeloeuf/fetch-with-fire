{
  "name": "fetch-with-fire",
  "version": "1.3.0",
  "description": "Extends browsers' \"fetch\" API with firebase auth built in",
  "main": "./src/index.js",
  "files": [
    "/dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "git stash --include-untracked && npx rimraf ./dist && npx webpack --config webpack.config.js && npm publish && git stash apply",
    "publish:npm": "npm run build && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Enkel-Digital/fetch-with-fire.git"
  },
  "keywords": [
    "fetch",
    "firebase",
    "auth",
    "jwt",
    "token"
  ],
  "author": "JJ",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Enkel-Digital/fetch-with-fire/issues"
  },
  "homepage": "https://github.com/Enkel-Digital/fetch-with-fire#readme",
  "dependencies": {
    "deepmerge": "^4.2.2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  }
}
